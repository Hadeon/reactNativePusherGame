Basic React Native application using Pusher for multiplayer games. 

.env is ignored will be need to setup prior to starting the application using Pusher credentials (APP_ID, APP_KEY, APP_SECRET, APP_CLUSTER)